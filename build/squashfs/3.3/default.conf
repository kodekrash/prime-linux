CFG_VERSION="3.3"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}squashfs/3.3"
CFG_SOURCE="squashfs3.3.tgz"
CFG_BUILDTMP="0"
CFG_BUILD="cd squashfs-tools && make && mkdir @TARGET/sbin && cp {mk,un}squashfs @TARGET/sbin/"

