CFG_VERSION="5.10.0"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}perl/5.10.0"
CFG_SOURCE="perl-5.10.0.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure.gnu --prefix=@DIRUSR -Dman1dir=@DIRMAN/man1 -Dman3dir=@DIRMAN/man3 -Dpager=/usr/bin/less -s -r && make && make install"
