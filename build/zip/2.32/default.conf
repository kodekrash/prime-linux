CFG_VERSION="2.32"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}zip/2.32"
CFG_SOURCE="zip-2.32.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="cp unix/Makefile ./ && make generic && make prefix=@TARGET@DIRUSR install"
