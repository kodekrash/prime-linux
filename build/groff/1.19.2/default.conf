CFG_VERSION="1.19.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}groff/1.19.2"
CFG_SOURCE="groff-1.19.2.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@TARGET@DIRUSR --sysconfdir=@TARGET@DIRETC --mandir=@TARGET@DIRMAN --infodir=@TARGET@DIRINFO --without-gs && mkdir @TARGET@DIRUSR && make && make install && mkdir -pv @TARGET@DIRETC/profile.d && cp -fv @CUSTOM/groff.sh @TARGET@DIRETC/profile.d/ && rm -frv @TARGET@DIRUSR/share/doc"
