CFG_VERSION="2.4.4"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}ppp/2.4.4"
CFG_SOURCE="ppp-2.4.4.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure && for f in \$(find . -name Makefile*); do sed -i -e 's|share/man|man|g' -e 's|usr/local|usr|g' \$f; done && make && make INSTROOT=@TARGET install"
