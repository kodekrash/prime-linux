CFG_VERSION="5.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}readline/5.2"
CFG_SOURCE="readline-5.2.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --libdir=@DIRLIB --enable-shared --disable-static && make SHLIB_LIBS=-lncurses && make install && mkdir -pv @TARGET@DIRUSR@DIRLIB && rm -fv @TARGET@DIRLIB/lib{readline,history}.so && cd @TARGET@DIRUSR@DIRLIB && ln -sfv ../..@DIRLIB/libreadline.so.5 libreadline.so && ln -sfv ../..@DIRLIB/libhistory.so.5 libhistory.so"
