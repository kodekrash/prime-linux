CFG_VERSION="1.0"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}primebase/1.0"
CFG_SOURCE="primebase-1.0.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="make install && make RELEASE=@RELEASE DESTDIR=@TARGET release"
