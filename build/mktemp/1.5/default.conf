CFG_VERSION="1.5"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}mktemp/1.5"
CFG_SOURCE="mktemp-1.5.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=@TARGET@DIRUSR --with-man && make && make install"
