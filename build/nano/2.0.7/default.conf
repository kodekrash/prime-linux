CFG_VERSION="2.0.7"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}nano/2.0.7"
CFG_SOURCE="nano-2.0.7.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-mouse --disable-speller --enable-color --enable-nanorc --disable-nls && make && make install && mkdir -p @TARGET@DIRETC/profile.d && cp @CUSTOM/nano.sh @TARGET@DIRETC/profile.d/"
