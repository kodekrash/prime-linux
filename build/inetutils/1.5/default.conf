CFG_VERSION="1.5"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}inetutils/1.5"
CFG_SOURCE="inetutils-1.5.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-servers --disable-clients --enable-ftp --enable-ping --enable-whois --enable-ipv6 --without-pam && make && make install"
