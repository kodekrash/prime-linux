CFG_VERSION="4.4.0"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}findutils/4.4.0"
CFG_SOURCE="findutils-4.4.0.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --localstatedir=@DIRVAR/cache/find --enable-threads=posix --disable-nls && make && make install"
