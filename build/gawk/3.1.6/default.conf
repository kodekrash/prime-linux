CFG_VERSION="3.1.6"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}gawk/3.1.6"
CFG_SOURCE="gawk-3.1.6.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --libexecdir=@DIRUSR/libexec --enable-switch --enable-portals --disable-nls && make && make install"
