CFG_VERSION="2.86"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}sysvinit/2.86"
CFG_SOURCE="sysvinit-2.86.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="mkdir -p @TARGET/{bin,sbin,etc,usr} && mkdir -p @TARGET@DIRUSR/{bin,include,man} && mkdir -p @TARGET@DIRMAN/{man1,man5,man8} && cd src && make && make ROOT=@TARGET MANDIR=@DIRMAN INFODIR=@DIRINFO install && cd .. && cp -Rv @CUSTOM/etc/* @TARGET@DIRETC/ && cp -Rv @CUSTOM/sbin/* @TARGET/sbin/"
