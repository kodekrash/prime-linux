CFG_VERSION="2.2.10"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}pciutils/2.2.10"
CFG_SOURCE="pciutils-2.2.10.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="sed -i -e 's|share/man|man|g' Makefile && make PREFIX=@DIRUSR ZLIB=yes && make PREFIX=@DIRUSR DESTDIR=@TARGET install"
