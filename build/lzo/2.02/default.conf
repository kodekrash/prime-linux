CFG_VERSION="2.02"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}lzo/2.02"
CFG_SOURCE="lzo-2.02.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/usr --enable-shared=yes --enable-static=no && make && make install"
