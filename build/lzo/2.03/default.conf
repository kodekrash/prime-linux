CFG_VERSION="2.03"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}lzo/2.03"
CFG_SOURCE="lzo-2.03.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-shared=yes --enable-static=yes && make && make install"
