CFG_VERSION="2.1.3"
CFG_SOURCE="slang-2.1.3.tar.bz2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --mandir=/usr/man --infodir=/usr/share/info --sysconfdir=/etc --without-png --without-onig --with-pcre && make && make install && rm -frv @TARGET/usr/share/doc/slsh/html"
CFG_FLAGC=""
CFG_FLAGCPP=""
