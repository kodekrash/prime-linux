CFG_VERSION="2.9"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}cpio/2.9"
CFG_SOURCE="cpio-2.9.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-mt --disable-nls && make && make install"
