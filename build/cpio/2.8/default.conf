CFG_VERSION="2.8"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}cpio/2.8"
CFG_SOURCE="cpio-2.8.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/usr --infodir=/usr/share/info --mandir=/usr/man --enable-mt --disable-nls && make && make install"
CFG_FLAGC="-O2"
CFG_FLAGCPP="${CFG_FLAGC}"
CFG_FLAGCXX="${CFG_FLAGC}"
