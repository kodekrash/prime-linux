CFG_VERSION="3.3-pre1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}module-init-tools/3.3-pre1"
CFG_SOURCE="module-init-tools-3.3-pre1.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --bindir=@DIRBIN --sbindir=@DIRSBIN --enable-zlib && sed -i -e 's|share/man|man|g' -e 's|-Bstatic -lz|-Bdynamic -lz|g' Makefile && make install"
