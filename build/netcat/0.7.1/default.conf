CFG_VERSION="0.7.1"
CFG_SOURCE="netcat-0.7.1.tar.bz2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}netcat/0.7.1"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-nls && make && make install"
CFG_FLAGC=""
CFG_FLAGCPP=""
