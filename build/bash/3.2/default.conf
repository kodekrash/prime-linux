CFG_VERSION="3.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}bash/3.2"
CFG_SOURCE="bash-3.2.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --bindir=/bin --sbindir=/sbin --libdir=/lib --enable-alias --enable-arith-for-command --enable-bang-history --enable-brace-expansion --enable-command-timing --enable-cond-command --enable-cond-regexp --enable-debugger --enable-history --enable-progcomp --enable-prompt-string-decoding --enable-restricted --disable-profiling && sed -i -e 's|charset=UTF-8|charset=KOI8-R|g' po/ru.po  && make && mkdir -p @TARGET/usr/info && make install && cd @TARGET/bin && ln -s bash sh"
