CFG_VERSION="1.0.4"
CFG_SOURCE="mcron-1.0.4.tar.gz"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}mcron/1.0.4"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --with-allow-file=@DIRETC/cron/allow 
--with-deny-file=@DIRETC/cron/deny && make && make install"
CFG_FLAGC=""
CFG_FLAGCPP=""
