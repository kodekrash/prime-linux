CFG_VERSION="2.5.35"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}flex/2.5.35"
CFG_SOURCE="flex-2.5.35.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-nls && make && make install && cp -fv @CUSTOM/lex @TARGET@DIRUSR@DIRBIN && chmod -v 755 @TARGET@DIRUSR@DIRBIN/lex"
