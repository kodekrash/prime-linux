CFG_VERSION="418"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}less/418"
CFG_SOURCE="less-418.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO && make && make install"
