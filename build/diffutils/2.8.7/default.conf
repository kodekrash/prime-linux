CFG_VERSION="2.8.7"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}diffutils/2.8.7"
CFG_SOURCE="diffutils-2.8.7.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --mandir=@DIRMAN --infodir=@DIRINFO --disable-nls && make && make install"
