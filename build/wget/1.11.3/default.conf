CFG_VERSION="1.11.3"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}wget/1.11.3"
CFG_SOURCE="wget-1.11.3.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-nls && sed -i 's|/usr/bin/env|/bin/env|g' doc/texi2pod.pl && make && make install"
