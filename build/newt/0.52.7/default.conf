CFG_VERSION="0.52.7"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}newt/0.52.7"
CFG_SOURCE="newt-0.52.7-prime.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --infodir=/usr/share/info --disable-nls && patch -p0 < @CUSTOM/@VERSION-disable_snack.patch && make && make install"
