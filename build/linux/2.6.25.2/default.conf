CFG_VERSION="2.6.25.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}linux/2.6.25.2"
CFG_SOURCE="linux-2.6.25.2.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="patch -p0 < @CUSTOM/@VERSION-gcc-4.3.patch && make mrproper && make headers_check && make INSTALL_HDR_PATH=@TARGET/usr headers_install && cp @CUSTOM/@VERSION-config ./.config && make oldconfig && make && make bzImage && make modules && make INSTALL_MOD_PATH=@TARGET modules_install && mkdir -p @TARGET/boot/@VERSION && cp System.map arch/i386/boot/bzImage @TARGET/boot/@VERSION/ && cp .config @TARGET/boot/@VERSION/config && cp @CUSTOM/@VERSION-grub @TARGET/boot/@VERSION/grub"
