CFG_VERSION="2.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}fbset/2.1"
CFG_SOURCE="fbset-2.1-prime.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="make && mkdir -p @TARGET/{etc,dev,usr/sbin,usr/man/man{5,8}} && make install && cp @CUSTOM/fb.modes @TARGET@DIRETC"
