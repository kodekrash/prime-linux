CFG_VERSION="2.02"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}nasm/2.02"
CFG_SOURCE="nasm-2.02.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO && make && mkdir -p @TARGET@DIRUSR/{bin,man/man1} && make INSTALLROOT=@TARGET install"
