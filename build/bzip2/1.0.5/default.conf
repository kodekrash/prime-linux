CFG_VERSION="1.0.5"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}bzip2/1.0.5"
CFG_SOURCE="bzip2-1.0.5.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="make -f Makefile-libbz2_so && make clean && make && make PREFIX=@TARGET@DIRUSR install && cp -fv libbz2.so.1.* @TARGET@DIRUSR@DIRLIB"
