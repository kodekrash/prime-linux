CFG_VERSION="7.7"
CFG_SOURCE="pcre-7.7.tar.bz2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}pcre/7.7"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --htmldir=@DIRJUNK --enable-shared --enable-static --enable-utf8 --enable-newline-is-lf --enable-pcregrep-libz --enable-pcregrep-libbz2 && make && make install"
