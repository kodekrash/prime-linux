CFG_VERSION="2.3"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}bison/2.3"
CFG_SOURCE="bison-2.3.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --infodir=@DIRINFO --mandir=@DIRMAN --disable-nls && make && make install"
