CFG_VERSION="2.3a"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}bison/2.3a"
CFG_SOURCE="bison-2.3a.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --infodir=@DIRINFO --mandir=@DIRMAN --disable-nls && echo '#define YYENABLE_NLS 1' >> config.h && make && make install"
