CFG_VERSION="0.28.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}neon/0.28.2"
CFG_SOURCE="neon-0.28.2.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/usr --mandir=/usr/man --infodir=/usr/info --enable-shared --disable-static && make && make install"
