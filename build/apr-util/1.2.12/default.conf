CFG_VERSION="1.2.12"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}apr-util/1.2.12"
CFG_SOURCE="apr-util-1.2.12.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/usr --infodir=/usr/info --mandir=/usr/man --with-apr=/usr && make && make install"
