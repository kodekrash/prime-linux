CFG_VERSION="22.6"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}psmisc/22.6"
CFG_SOURCE="psmisc-22.6.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --dataroot=@DIRUSR --enable-ipv6 --disable-nls && make && make install"
