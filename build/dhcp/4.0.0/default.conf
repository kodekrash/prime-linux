CFG_VERSION="4.0.0"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}dhcp/4.0.0"
CFG_SOURCE="dhcp-4.0.0.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --infodir=/usr/share/info --mandir=/usr/man && make && make install"
