CFG_VERSION="1.8.5"
CFG_SOURCE="guile-1.8.5.tar.gz"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}guile/1.8.5"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --with-threads --disable-nls 
--disable-elisp --disable-deprecated && make && make install"
CFG_FLAGC=""
CFG_FLAGCPP=""
