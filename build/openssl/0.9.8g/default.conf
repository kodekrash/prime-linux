CFG_VERSION="0.9.8g"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}openssl/0.9.8g"
CFG_SOURCE="openssl-0.9.8g.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="./config --openssldir=@DIRETC/ssl --prefix=@DIRUSR zlib-dynamic shared && make MANDIR=@DIRMAN INFODIR=@DIRINFO && make INSTALL_PREFIX=@TARGET install MANDIR=@DIRMAN INFODIR=@DIRINFO"
