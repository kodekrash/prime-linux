CFG_VERSION="1.14"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}popt/1.14"
CFG_SOURCE="popt-1.14.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-static --enable-shared --disable-nls && make && make install"
