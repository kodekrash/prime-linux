CFG_VERSION="1.10.4"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}popt/1.10.4"
CFG_SOURCE="popt-1.10.4.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --mandir=/usr/man --infodir=/usr/info --enable-static --enable-shared && make && make install"
