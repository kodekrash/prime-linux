CFG_VERSION="4.0.18.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}shadow/4.0.18.1"
CFG_SOURCE="shadow-4.0.18.1-prime.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-shared=yes --enable-static=yes --disable-nls --without-selinux && sed -i 's|groups ||' src/Makefile && sed -i '/groups/d' \$(find man -name Makefile) && sed -i 's|/var/spool/mail|/var/mail|' ../shadow-4.0.18.1/etc/login.defs && make && make install && mkdir -p @TARGET@DIRETC && cp @CUSTOM/{passwd,group} @TARGET@DIRETC/ && sed -i -e 's|GROUP=1000|GROUP=100|g' -e 's|HOME=/home/users|HOME=/home|g' @TARGET@DIRETC/default/useradd && mkdir -p @TARGET@DIRETC/skel"
