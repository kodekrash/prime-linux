CFG_VERSION="1.95"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}grub/1.95"
CFG_SOURCE="grub-1.95.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --exec-prefix=/ --infodir=/usr/info --mandir=/usr/man && sed -i 's|{ state->err = 0} newlines commands|{ state->err = 0;} newlines commands|g' normal/parser.y  && make && make install && mkdir -p @TARGET/boot/grub && cp -f @CUSTOM/grub.cfg @TARGET/boot/grub/"
