CFG_VERSION="6.9.92"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}coreutils/6.9.92"
CFG_SOURCE="coreutils-6.9.92.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/ --datarootdir=/usr --datadir=/usr --disable-nls && make && make install && mkdir -p @TARGET/etc/profile.d && cp -f @CUSTOM/colorrc @TARGET/etc/ && cp -f @CUSTOM/coreutils.sh @TARGET/etc/profile.d/ && chmod +x @TARGET/etc/profile.d/coreutils.sh"
