CFG_VERSION="1.10.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}automake/1.10.1"
CFG_SOURCE="automake-1.10.1.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --mandir=@DIRMAN --infodir=@DIRINFO && make && make install"
