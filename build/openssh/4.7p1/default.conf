CFG_VERSION="4.7p1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}openssh/4.7p1"
CFG_SOURCE="openssh-4.7p1.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --prefix=/usr --sysconfdir=/etc/ssh --libexecdir=/usr/libexec --infodir=/usr/share/info --mandir=/usr/man --with-mantype=man --without-pam --without-selinux --with-md5-passwords && make && make install"
