CFG_VERSION="5.0p1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}openssh/5.0p1"
CFG_SOURCE="openssh-5.0p1.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC/ssh --mandir=@DIRMAN --infodir=@DIRINFO --libexecdir=@DIRUSR/libexec --with-mantype=man --without-pam --without-selinux --with-md5-passwords && make && make install"
