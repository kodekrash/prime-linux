CFG_VERSION="1.4.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}iptables/1.4.1"
CFG_SOURCE="iptables-1.4.1.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --libdir=@DIRUSR@DIRLIB --bindir=@DIRBIN --mandir=@DIRMAN --infodir=@DIRINFO --with-xtlibdir=@DIRUSR@DIRLIB/iptables --enable-devel --enable-shared --disable-static && make && make install"
