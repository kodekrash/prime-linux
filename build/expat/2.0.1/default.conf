CFG_VERSION="2.0.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}expat/2.0.1"
CFG_SOURCE="expat-2.0.1.tar.gz"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-shared --disable-static && make && make install"
