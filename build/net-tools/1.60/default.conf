CFG_VERSION="1.60"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}net-tools/1.60"
CFG_SOURCE="net-tools-1.60-prime.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="sed -i 's|share/man|man|g' man/Makefile && make && make BASEDIR=@TARGET install"
