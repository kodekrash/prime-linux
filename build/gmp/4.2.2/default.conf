CFG_VERSION="4.2.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}gmp/4.2.2"
CFG_SOURCE="gmp-4.2.2.tar.bz2"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-shared=yes --enable-static=no 
--enable-cxx --disable-nls ABI=32 && make && make install"
