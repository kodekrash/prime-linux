CFG_VERSION="1.40.8"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}e2fsprogs/1.40.8"
CFG_SOURCE="e2fsprogs-1.40.8.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --exec-prefix=/ --enable-elf-shlibs --disable-profile --enable-fsck --enable-dynamic-e2fsck --enable-initrd-helper --disable-nls && make && make install && make install-libs"
