CFG_VERSION="1.40.2"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}e2fsprogs/1.40.2"
CFG_SOURCE="e2fsprogs-1.40.2.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=/usr --exec-prefix=/ --sysconfdir=/etc --enable-elf-shlibs --disable-profile --enable-fsck --enable-dynamic-e2fsck --enable-initrd-helper && make && make install && make install-libs"
