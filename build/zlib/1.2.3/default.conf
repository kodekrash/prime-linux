CFG_VERSION="1.2.3"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}zlib/1.2.3"
CFG_SOURCE="zlib-1.2.3.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="configure --prefix=@DIRUSR --libdir=@DIRLIB --shared && sed -i -e 's|share/man|man|g' -e 's|libdir =/lib|libdir=\${prefix}/lib|g' Makefile && make && make prefix=@TARGET@DIRUSR install"
