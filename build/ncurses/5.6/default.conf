CFG_VERSION="5.6"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}ncurses/5.6"
CFG_SOURCE="ncurses-5.6-prime.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --without-ada --with-shared --without-normal --without-profile --enable-symlinks --enable-widec --without-debug --enable-overwrite && make && make install && mkdir -p @TARGET@DIRLIB && cd @TARGET@DIRLIB && ln -s ..@DIRUSR@DIRLIB/libncursesw.so.5 && ln -s ..@DIRUSR@DIRLIB/libncursesw.so.5.6 && cd @TARGET@DIRUSR@DIRLIB && ln -s libncursesw.so.5.6 libncurses.so.5.6 && ln -s libncursesw.so.5.6 libncurses.so.5 && ln -s libncursesw.so.5.6 libncurses.so && ln -s libncursesw.so.5.6 libcurses.so && mkdir -pv @TARGET@DIRETC/profile.d && cp -vf @CUSTOM/ncurses.sh @TARGET@DIRETC/profile.d/"
