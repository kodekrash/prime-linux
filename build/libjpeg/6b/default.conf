CFG_VERSION="6b"
CFG_SOURCE="jpegsrc.v6b.tar.gz"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}libjpeg/6b"
CFG_BUILDTMP="1"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --enable-shared=yes --enable-static=no && patch -p0 < @CUSTOM/@VERSION-makefile_destdir.patch && make && make install && make install-lib && make install-headers"
CFG_FLAGC=""
CFG_FLAGCPP=""
