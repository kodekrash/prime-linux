CFG_VERSION="7.1"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}vim/7.1"
CFG_SOURCE="vim-7.1.tar.bz2"
CFG_BUILDTMP="0"
CFG_BUILD="configure --host=@ARCH --prefix=@DIRUSR --sysconfdir=@DIRETC --mandir=@DIRMAN --infodir=@DIRINFO --disable-gtktest --disable-gpm --disable-nls --enable-multibyte && sed -i 's|^DOSBIN_S|#DOSBIN_S|g' Makefile && make VIMRCLOC=@DIRETC VIMRUNTIMEDIR=@DIRSHARE/vim MAKE='make -e' && make install && mkdir -p @TARGET@DIRETC/profile.d && cp -fv @CUSTOM/vim.sh @TARGET@DIRETC/profile.d/ && cp -fv @CUSTOM/vimrc @TARGET@DIRETC/"
