CFG_VERSION="1.07"
CFG_PACKAGER="${REPO_PACKAGER}"
CFG_URL="${REPO_URL}mingetty/1.07"
CFG_SOURCE="mingetty-1.07.tar.gz"
CFG_BUILDTMP="0"
CFG_BUILD="make && sed -i 's|share/man|man|g' Makefile && mkdir -p 
@TARGET/usr/man/man8 && mkdir -p @TARGET/sbin && make install"
